#!/usr/bin/env bash

# Iterate over every item in the current directory
for item in *; do
  # Check if the item is a symbolic link
  if [ -L "$item" ]; then
    # Use rm to remove the symlink
    rm "$item"
    echo "Removed symlink: $item"
  fi
done
